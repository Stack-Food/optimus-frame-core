# ATENÇÃO: Não commite este arquivo com valores reais preenchidos.
# O deploy-eks.ps1 sobrescreve este secret via kubectl durante o deploy.
apiVersion: v1
kind: Secret
metadata:
  name: optimus-frame-core-secret
  labels:
    app.kubernetes.io/name: optimus-frame-core
    app.kubernetes.io/part-of: optimus-frame
type: Opaque
stringData:
  # Banco de dados PostgreSQL (RDS)
  ConnectionStrings__DefaultConnection: "Host=optimus-frame-db.cx5yyxox09ju.us-east-1.rds.amazonaws.com;Port=5432;Database=optimus_frame_db;Username=optimusframe;Password=postgres"

  # RabbitMQ
  RabbitMQ__UserName: "REPLACE_WITH_RABBITMQ_USER"
  RabbitMQ__Password: "Optimus-Frame@2026"

  # Credenciais AWS temporárias (session token)
  # Substituídas em runtime pelo deploy-eks.ps1
  AWS_ACCESS_KEY_ID: "REPLACE_WITH_ACCESS_KEY_ID"
  AWS_SECRET_ACCESS_KEY: "REPLACE_WITH_SECRET_ACCESS_KEY"
  AWS_SESSION_TOKEN: "REPLACE_WITH_SESSION_TOKEN"
